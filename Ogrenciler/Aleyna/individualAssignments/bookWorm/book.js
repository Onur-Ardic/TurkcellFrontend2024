class Book {
  constructor(id, title, author, year, genre, image) {
    (this.id = id),
      (this.title = title),
      (this.author = author),
      (this.year = year),
      (this.genre = genre),
      (this.image = image);
  }
}
